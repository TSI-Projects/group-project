<UserControl x:Class="GW_UI.UserControls.MenuButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" d:DesignWidth="50" Name="menuButton">
    <Grid>
        <Button Name="btn" Style="{StaticResource menuButton}" PreviewMouseUp="OnMouseUp">
            <StackPanel Orientation="Horizontal">
                <Icon:PackIconMaterial Kind="{Binding ElementName=menuButton,Path=Icon}" Style="{StaticResource menuButtonIcon}"/>
            </StackPanel>
        </Button>

    </Grid>
</UserControl>
